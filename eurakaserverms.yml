server:
  port: 8761
eureka:
  instance:
    prefer-ip-address: false
    hostname: localhost   # or the container name if you want intra-docker calls
    instance-id: ${spring.application.name}:${random.value}

            
